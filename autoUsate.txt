consegna

Modellizzare la struttura di una tabella per memorizzare tutti i dati riguardanti delle auto usate messe in vendita da un concessionario

nome tabella: USED CARS

ID                                          BIGINT              UNIQUE, AUTOINCREMENT, NOTNULL        PRIMARY KEY
YEAR                                        YEAR                NOTNULL, DEFAULT ("0000")
GENRE                                       VARCHAR(100)        NOTNULL, DEFAULT("unknown")
COLOR                                       VARCHAR(100)        NOTNULL, DEFAULT("unknown")
CONDITION                                   VARCHAR(100)        NOTNULL
VIN(VEHICLE IDENTIFICATION NUMBER)          CHAR(17)            NOTNULL, UNIQUE
CATEGORY                                    VARCHAR(100)        NOTNULL
BRAND                                       VARCHAR(100)        NOTNULL
MODEL                                       VARCHAR(100)        NOTNULL
NUMBER_OF_OWNER                             TINYINT(10)         NOTNULL
FUEL_TYPE                                   VARCHAR(50)         NOTNULL
